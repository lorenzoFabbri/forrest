% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forrest.R
\name{forrest}
\alias{forrest}
\title{Create a forest plot}
\usage{
forrest(
  data,
  estimate,
  lower,
  upper,
  label = NULL,
  group = NULL,
  is_summary = NULL,
  weight = NULL,
  ref_line = 0,
  log_scale = FALSE,
  xlim = NULL,
  xlab = "Estimate (95\% CI)",
  title = NULL,
  header = NULL,
  cols = NULL,
  widths = NULL,
  stripe = FALSE,
  dodge = FALSE,
  pch = 15,
  shape = NULL,
  lwd = 2,
  cex = 1,
  col = NULL,
  cols_by_group = FALSE,
  legend_pos = "topright",
  legend_shape_pos = "bottomright",
  theme = "default",
  ...
)
}
\arguments{
\item{data}{A data frame, tibble, or data.table.}

\item{estimate}{Column name (string) for point estimates.}

\item{lower}{Column name (string) for lower confidence interval bounds.}

\item{upper}{Column name (string) for upper confidence interval bounds.}

\item{label}{Column name (string) for row labels displayed on the y-axis or
in the left text panel. If \code{NULL}, row numbers are used.}

\item{group}{Column name (string) for a grouping variable. Rows that share
a group value receive the same colour, and a legend is drawn automatically.}

\item{is_summary}{Column name (string) of a logical vector. Rows where this
is \code{TRUE} are drawn as filled diamonds (e.g. pooled or overall estimates)
rather than squares with whiskers.}

\item{weight}{Column name (string) of numeric row weights. When provided,
point size scales as \code{cex * sqrt(weight / max(weight))}, so rows with
larger weights appear with a bigger marker. Weights for summary rows are
ignored (diamond size is fixed by \code{cex}).}

\item{ref_line}{Numeric. Position of the vertical reference line (e.g. \code{0}
for differences, \code{1} for ratio measures on the natural scale). Set to
\code{NULL} to suppress. Default is \code{0}.}

\item{log_scale}{Logical. If \code{TRUE}, apply a log transformation to the
x-axis. Useful when plotting odds ratios, hazard ratios, or risk ratios
on the natural scale. Default is \code{FALSE}.}

\item{xlim}{Numeric vector of length 2 giving x-axis limits. Computed from
the data when \code{NULL} (default). Confidence intervals that extend beyond
\code{xlim} are clipped at the axis boundary and an arrow is drawn to indicate
truncation.}

\item{xlab}{Label for the x-axis. Default is \code{"Estimate (95\% CI)"}.}

\item{title}{Plot title. Default is \code{NULL} (no title).}

\item{header}{Optional header string placed above the label column. When
\code{cols} is provided this appears above the left text panel; otherwise it is
drawn above the topmost row on the y-axis.}

\item{cols}{Named character vector specifying extra text columns to display
to the right of the plot. Names become column headers; values are column
names in \code{data}. Example: \code{cols = c("OR (95\\\\\% CI)" = "or_ci")}.}

\item{widths}{Numeric vector of relative panel widths. When \code{cols} is
\code{NULL}, ignored. Otherwise, length must equal \code{2 + length(cols)}: label
panel, plot panel, then one entry per extra column. Sensible defaults are
chosen automatically.}

\item{stripe}{Logical. If \code{TRUE}, alternate rows are shaded with a light
grey background to improve readability. Default is \code{FALSE}.}

\item{dodge}{Logical or positive numeric. When \code{TRUE} (or a positive
number), consecutive rows that share the same \code{label} value are grouped
together and their confidence intervals are drawn with a small vertical
offset so that they do not overlap. The shared label is displayed once at
the centre of the group. Use together with \code{group} (for colour) and/or
\code{shape} (for point characters) to distinguish the overlaid series. A
numeric value sets the offset between rows in a group directly (in y-axis
units); \code{TRUE} uses a default of \code{0.25}. Default is \code{FALSE}.}

\item{pch}{Point character for non-summary rows. Default is \code{15} (filled
square). When \code{shape} is provided, \code{pch} is used only as a fallback for
rows whose shape value is \code{NA}.}

\item{shape}{Column name (string) for a shape variable. When provided,
different values of the column are rendered with different point characters
and a shape legend is drawn. Use together with \code{group} to distinguish two
categorical dimensions simultaneously (e.g. colour = time period, shape =
sex).}

\item{lwd}{Line width for confidence interval whiskers. Default is \code{2}.}

\item{cex}{Point size multiplier. Default is \code{1}.}

\item{col}{Colour or character vector of colours. When \code{NULL} (default) and
\code{group} is specified, the Okabe-Ito colorblind-safe palette is used. When
\code{NULL} and no \code{group}, a single dark colour is used.}

\item{cols_by_group}{Logical. Relevant only when \code{dodge} is active. When
\code{TRUE}, each text column in \code{cols} is collapsed to one value per label
group: the first non-empty entry within the group is displayed at the
group centre y position. This produces a wide-format text table with one
row per label and one column per condition. Populate each text column so
that the value is non-empty only for the matching condition row and empty
(\code{""}) for all others; \code{forrest()} picks up the right value automatically.
When \code{FALSE} (default), text values are drawn at each individual row's
dodged y position, keeping them aligned with their CI whiskers.}

\item{legend_pos}{Position of the colour legend when \code{group} is supplied.
Passed to \code{legend()}. Use \code{NULL} to suppress.
Default is \code{"topright"}.}

\item{legend_shape_pos}{Position of the shape legend when \code{shape} is
supplied. Passed to \code{legend()}. Use \code{NULL} to suppress.
Default is \code{"bottomright"}.}

\item{theme}{Visual theme name ("default", "minimal", "classic") or a
named list of style overrides. Default is \code{"default"}.}

\item{...}{Graphical parameters forwarded to the internal tinyplot call
(e.g. \code{cex.axis}, \code{cex.lab}).}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing a
plot.
}
\description{
Draws a publication-ready forest plot from a data frame. Each row
represents one estimate — a study, a predictor, a model, a subgroup, or any
other unit of analysis. Rows whose \code{estimate} is \code{NA} are treated as
spacer/header rows: they produce no point or confidence interval, and their
label is rendered in bold if non-empty.
}
\examples{
# Basic forest plot: linear model coefficients
dat <- data.frame(
  predictor = c("Age (per 10 y)", "Female sex",
                "BMI (per 5 kg/m\u00b2)", "Current smoker"),
  estimate  = c(0.42, -0.38,  0.19, -0.31),
  lower     = c(0.22, -0.56, -0.02, -0.51),
  upper     = c(0.62, -0.20,  0.40, -0.11)
)
forrest(dat,
  estimate = "estimate",
  lower    = "lower",
  upper    = "upper",
  label    = "predictor",
  xlab     = "Regression coefficient (95\% CI)"
)

# Multiple estimates per row (dodge) - same predictor in two time windows
dat2 <- data.frame(
  predictor = rep(c("Air pollution (PM2.5)",
                    "Sedentary behaviour",
                    "Diet quality score"), each = 2),
  period    = rep(c("Childhood", "Adulthood"), 3),
  estimate  = c( 0.18,  0.30, -0.12, -0.22,  0.09,  0.14),
  lower     = c(-0.02,  0.10, -0.30, -0.40, -0.08, -0.04),
  upper     = c( 0.38,  0.50,  0.06, -0.04,  0.26,  0.32)
)
forrest(dat2,
  estimate = "estimate",
  lower    = "lower",
  upper    = "upper",
  label    = "predictor",
  group    = "period",
  dodge    = TRUE,
  xlab     = "Regression coefficient (95\% CI)"
)

}
