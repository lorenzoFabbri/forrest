% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_forrest}
\alias{save_forrest}
\title{Save a forest plot to a file}
\usage{
save_forrest(file, plot, width = 7, height = 5,
             dpi = 300, bg = "white")
}
\arguments{
\item{file}{Output file path. Supported extensions: \code{.pdf}, \code{.png}, \code{.svg},
\code{.tiff}.}

\item{plot}{A zero-argument function whose body calls \code{\link[=forrest]{forrest()}}.
Evaluated inside the open graphics device.}

\item{width}{Plot width in inches. Default \code{7}.}

\item{height}{Plot height in inches. Default \code{5}.}

\item{dpi}{Resolution in dots per inch for raster formats (\code{.png}, \code{.tiff}).
Ignored for vector formats (\code{.pdf}, \code{.svg}). Default \code{300}.}

\item{bg}{Background colour. Default \code{"white"}.}
}
\value{
Invisibly returns \code{file}.
}
\description{
Writes the plot produced by \code{\link[=forrest]{forrest()}} (or any base-R plotting code) to a
file. The graphics device is inferred from the file extension.
}
\examples{
dat <- data.frame(
  label    = c("Age (per 10 y)", "Female sex", "Current smoker"),
  estimate = c(0.42, -0.38, -0.31),
  lower    = c(0.22, -0.56, -0.51),
  upper    = c(0.62, -0.20, -0.11)
)
tmp <- tempfile(fileext = ".pdf")
save_forrest(tmp, function() {
  forrest(
    dat,
    estimate = "estimate",
    lower    = "lower",
    upper    = "upper",
    label    = "label",
    xlab     = "Regression coefficient (95\% CI)"
  )
})

}
